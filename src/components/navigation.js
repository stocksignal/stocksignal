import React from "react"
import ThemeChanger from "../components/themeChanger"

export default (props) => (
  <nav className="navigation"> 
    <a href="https://stocksignal.github.io">Home</a>
    <ThemeChanger/>
  </nav>
  
)
